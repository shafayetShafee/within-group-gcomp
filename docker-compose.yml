
services:
  rstudio:
    image: ghcr.io/shafayetshafee/within-group-gcomp:0.0.9
    container_name: within-group-gcomp
    ports:
      - "8787:8787"
    environment:
      DISABLE_AUTH: "true"
    restart: unless-stopped
